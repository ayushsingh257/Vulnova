apiVersion: v1
kind: Service
metadata:
  name: vulnova-backend-service
  namespace: vulnova-prod
  labels:
    app.kubernetes.io/name: vulnova
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: vulnova
    app.kubernetes.io/component: backend
