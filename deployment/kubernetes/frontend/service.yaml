apiVersion: v1
kind: Service
metadata:
  name: vulnova-frontend-service
  namespace: vulnova-prod
  labels:
    app.kubernetes.io/name: vulnova
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: vulnova
    app.kubernetes.io/component: frontend
